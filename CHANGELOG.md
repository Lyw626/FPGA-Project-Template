# 更新记录

## 未发布

- 将 `config/`、`scripts/`、`releases/` 统一收纳到 `project-control/`。
- 使用临时 Git 索引检查待提交文件稳定性，兼容中文路径和 VS Code PowerShell 终端。
- 增加仓库级 `$sync-fpga-code` Codex 同步 Agent。
- 将 `$sync-fpga-code` 调整为一次确认后自动部署整个仓库。

## 2026-07-14

- 创建 FPGA 多电脑开发模板。
- 增加 Git LFS、工作分支、环境检查和自动发布流程。
